# frozen_string_literal: true

source "https://rubygems.org"

# Specify your gem's dependencies in anvil.gemspec
gemspec

gem "irb"
gem "rake", "~> 13.0"

gem "minitest", "~> 5.16"

gem "standard", "~> 1.3"

# Optional companion: anvil registers its locals with the_local when present.
# anvil works standalone (the registration is guarded), so this is dev/test-only.
gem "the_local", github: "tylercschneider/the_local"
